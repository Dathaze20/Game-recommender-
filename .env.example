# Copy this file to `.env` and fill in your own RAWG API key.
# Get a free key at https://rawg.io/apikey
#
# `.env` is git-ignored and is loaded automatically when python-dotenv is
# installed (it ships in requirements.txt).

GAME_API_KEY=
